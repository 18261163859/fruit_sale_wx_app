/* pages/category/category.wxss */
page {
  height: 100%;
  background-color: #f5f5f5;
}

.container {
  height: 100%;
  width: 100%;
  display: flex;
  flex-direction: column;
  padding-top: constant(safe-area-inset-top);
  padding-top: env(safe-area-inset-top);
}

/* 顶部标题 */
.header {
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  padding: 24rpx 0;
  text-align: center;
  flex-shrink: 0;
  box-shadow: 0 4rpx 16rpx rgba(102, 126, 234, 0.2);
}

/* 新年主题标题栏 */
.header[data-theme="newyear"] {
  background: linear-gradient(135deg, #E63946 0%, #C1121F 100%);
  box-shadow: 0 4rpx 16rpx rgba(230, 57, 70, 0.3),
              0 0 12rpx rgba(255, 215, 0, 0.2);
  border-bottom: 3rpx solid #FFD700;
}

.header-title {
  font-size: 36rpx;
  font-weight: bold;
  color: #fff;
  letter-spacing: 2rpx;
}

.header[data-theme="newyear"] .header-title {
  text-shadow: 0 2rpx 8rpx rgba(0, 0, 0, 0.3),
               0 0 10rpx rgba(255, 215, 0, 0.5);
}

/* 分类区域容器 */
.category-wrapper {
  flex: 1;
  display: flex;
  overflow: hidden;
  width: 100%;
}

/* 左侧分类列表 */
.category-list {
  width: 200rpx;
  height: 100%;
  flex-shrink: 0;
  background-color: #fafafa;
}

.category-item {
  width: 100%;
  padding: 36rpx 20rpx;
  text-align: center;
  background-color: transparent;
  box-sizing: border-box;
  transition: all 0.3s ease;
  position: relative;
}

.category-item.active {
  background-color: #fff;
}

.category-name {
  font-size: 28rpx;
  color: #666;
  word-break: keep-all;
  white-space: nowrap;
  transition: all 0.3s ease;
  font-weight: 500;
}

.category-item.active .category-name {
  font-weight: bold;
  font-size: 30rpx;
}

.active-indicator {
  position: absolute;
  left: 0;
  top: 50%;
  transform: translateY(-50%);
  width: 6rpx;
  height: 40rpx;
  border-radius: 0 4rpx 4rpx 0;
}

/* 右侧商品区域 */
.product-wrapper {
  width: calc(100% - 200rpx);
  flex-shrink: 0;
  display: flex;
  flex-direction: column;
  background-color: #f5f5f5;
  overflow: hidden;
}

/* 商品头部（分类名+排序） */
.product-header {
  width: 100%;
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 24rpx 30rpx;
  background-color: transparent;
  flex-shrink: 0;
  box-sizing: border-box;
}

.category-title {
  font-size: 32rpx;
  font-weight: bold;
  color: #333;
}

.sort-selector {
  display: flex;
  align-items: center;
  padding: 10rpx 20rpx;
  border-radius: 50rpx;
  background: #f5f5f5;
}

.sort-text {
  font-size: 26rpx;
  color: #666;
  margin-right: 8rpx;
}

.sort-arrow {
  font-size: 20rpx;
  color: #999;
}

/* 商品列表 */
.product-list {
  flex: 1;
  width: 100%;
  height: 100%;
  box-sizing: border-box;
  padding: 10rpx;
}

/* 商品卡片 - 横向布局 */
.product-card {
  display: flex;
  align-items: stretch;
  background-color: #fff;
  margin: 10rpx;
  border-radius: 16rpx;
  box-shadow: 0 2rpx 12rpx rgba(0,0,0,0.06);
  position: relative;
  transition: all 0.3s ease;
  box-sizing: border-box;
  overflow: hidden;
}

/* 新年主题商品卡片 */
.product-card[data-theme="newyear"] {
  border: 2rpx solid #FFD700;
  box-shadow: 0 4rpx 16rpx rgba(230, 57, 70, 0.12),
              0 0 8rpx rgba(255, 215, 0, 0.15);
  background: linear-gradient(to right, #FFFEF7 0%, #FFF8F0 100%);
}

.product-card[data-theme="newyear"]::before {
  content: '🎊';
  position: absolute;
  top: 10rpx;
  left: 10rpx;
  font-size: 32rpx;
  opacity: 0.3;
  z-index: 1;
}

.product-card:active {
  transform: scale(0.98);
  box-shadow: 0 4rpx 16rpx rgba(0,0,0,0.1);
}

/* 商品图片 */
.product-img {
  width: 200rpx;
  height: 200rpx;
  background-color: #f5f5f5;
  flex-shrink: 0;
}

/* 商品内容 */
.product-content {
  flex: 1;
  padding: 20rpx;
  display: flex;
  flex-direction: column;
  justify-content: space-between;
  min-width: 0;
}

/* 商品标题 */
.product-title {
  font-size: 30rpx;
  font-weight: 600;
  color: #333;
  line-height: 1.4;
  margin-bottom: 8rpx;
  display: -webkit-box;
  -webkit-box-orient: vertical;
  -webkit-line-clamp: 2;
  overflow: hidden;
  word-break: break-all;
}

/* 规格描述 */
.product-spec {
  font-size: 24rpx;
  color: #999;
  margin-bottom: 12rpx;
  display: -webkit-box;
  -webkit-box-orient: vertical;
  -webkit-line-clamp: 1;
  overflow: hidden;
}

/* 价格行 */
.price-line {
  margin-bottom: 10rpx;
}

.price-group {
  display: flex;
  align-items: center;
  flex-wrap: wrap;
  gap: 10rpx;
}

/* 普通价格 */
.product-price {
  font-size: 36rpx;
  font-weight: bold;
  color: #ff6b35;
}

/* VIP价格 */
.vip-price {
  font-size: 36rpx;
  font-weight: bold;
  color: #ff6b35;
}

/* 原价 */
.original-price {
  font-size: 24rpx;
  color: #bbb;
  text-decoration: line-through;
}

/* VIP标签 */
.vip-tag {
  padding: 4rpx 12rpx;
  border-radius: 8rpx;
  display: inline-flex;
  align-items: center;
  justify-content: center;
}

.vip-tag-text {
  font-size: 20rpx;
  color: #fff;
  font-weight: bold;
}

/* 销量 */
.sales-text {
  font-size: 24rpx;
  color: #999;
}

/* 加购物车按钮 */
.add-cart-btn {
  position: absolute;
  right: 16rpx;
  bottom: 16rpx;
  width: 56rpx;
  height: 56rpx;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  box-shadow: 0 4rpx 12rpx rgba(0,0,0,0.15);
  transition: all 0.3s ease;
  z-index: 10;
}

.add-cart-btn:active {
  transform: scale(0.9);
}

.btn-text {
  color: #fff;
  font-size: 40rpx;
  font-weight: bold;
  line-height: 1;
}

/* 空状态 */
.empty-state {
  width: 100%;
  padding: 120rpx 0;
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  box-sizing: border-box;
}

.empty-icon {
  font-size: 120rpx;
  margin-bottom: 20rpx;
  opacity: 0.3;
}

.empty-text {
  font-size: 28rpx;
  color: #999;
}

# 生产环境配置
spring:
  datasource:
    url: jdbc:mysql://your-prod-host:3306/fruit_sale?useUnicode=true&characterEncoding=utf-8&useSSL=true&serverTimezone=Asia/Shanghai
    username: ${DB_USERNAME:fruit_admin}
    password: ${DB_PASSWORD:fruit123456}

  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
      password: ${REDIS_PASSWORD:}

logging:
  level:
    com.fruit.sale: info
  file:
    name: /var/log/fruit-sale/application.log
